{"chunk-0": "<!DOCTYPE html><html lang=\"en\"><head> <meta charset=\"UTF-8\"> <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\"> <title>Verus Blaster</title> <style>*{margin:0;padding:0;box-sizing:border-box;}body{margin:0;padding:0;display:flex;flex-direction:column;justify-content:center;align-items:center;min-height:100vh;background-color:#222;overflow:hidden;}#game-container{display:flex;flex-direction:column;align-items:center;gap:10px;}canvas{border:2px solid #fff;background-color:#050a0f;max-width:100%;max-height:100vh;display:block;}#touch-controls{display:none;width:100%;max-width:800px;height:150px;position:relative;background-color:rgba(34,34,34,0.8);border:2px solid #444;border-radius:10px;}@media (hover:none) and (pointer:coarse){#touch-controls{display:block;}}.joystick{position:absolute;left:40px;top:50%;transform:translateY(-50%);width:120px;height:120px;}.joystick-base{position:absolute;width:120px;height:120px;border-radius:50%;background-color:rgba(255,255,255,0.2);border:2px solid rgba(255,255,255,0.4);}.joystick-handle{position:absolute;width:50px;height:50px;border-radius:50%;background-color:rgba(255,255,255,0.5);left:50%;top:50%;transform:translate(-50%,-50%);transition:all 0.1s;pointer-events:none;}.shoot-button{position:absolute;right:40px;top:50%;transform:translateY(-50%);width:80px;height:80px;border-radius:50%;background-color:rgba(255,255,255,0.2);border:3px solid rgba(255,255,255,0.4);display:flex;align-items:center;justify-content:center;cursor:pointer;user-select:none;}.shoot-button.active{background-color:rgba(255,255,255,0.4);border-color:rgba(255,255,255,0.7);}.shoot-button::before,.shoot-button::after{content:'';position:absolute;background-color:rgba(255,255,255,0.6);}.shoot-button::before{width:30px;height:3px;}.shoot-button::after{width:3px;height:30px;}.shoot-button.active::before,.shoot-button.active::after{background-color:rgba(255,255,255,0.9);}.pause-button{position:absolute;left:calc(55%+5px);bottom:10px;transform:translateX(-50%);width:50px;height:50px;border-radius:50%;background-color:rgba(255,255,255,0.2);border:3px solid rgba(255,255,255,0.4);display:flex;align-items:center;justify-content:center;cursor:pointer;user-select:none;}.pause-button.active{background-color:rgba(255,255,255,0.4);border-color:rgba(255,255,255,0.7);}.pause-icon{width:20px;height:24px;position:relative;display:flex;gap:6px;}.pause-icon::before,.pause-icon::after{content:'';width:6px;height:24px;background-color:rgba(255,255,255,0.6);}.pause-button.active .pause-icon::before,.pause-button.active .pause-icon::after{background-color:rgba(255,255,255,0.9);}</style></head><body> <div id=\"game-container\"> <canvas id=\"canvas\" width=\"800\" height=\"600\"></canvas> <div id=\"touch-controls\"> <div class=\"joystick\" id=\"joystick\"> <div class=\"joystick-base\"></div> <div class=\"joystick-handle\" id=\"joystick-handle\"></div> </div> <div class=\"pause-button\" id=\"pause-button\"> <div class=\"pause-icon\"></div> </div> <div class=\"shoot-button\" id=\"shoot-button\"></div> </div> </div> <script>const canvas=document.getElementById('canvas');const ctx=canvas.getContext('2d');let audioContext=null;function initAudio(){if(!audioContext){audioContext=new(window.AudioContext||window.webkitAudioContext)();}}function playShootSound(){try{if(!audioContext)return;const now=audioContext.currentTime;const oscillator=audioContext.createOscillator();const gainNode=audioContext.createGain();oscillator.connect(gainNode);gainNode.connect(audioContext.destination);oscillator.frequency.setValueAtTime(800,now);oscillator.frequency.exponentialRampToValueAtTime(200,now+0.1);gainNode.gain.setValueAtTime(0.3,now);gainNode.gain.exponentialRampToValueAtTime(0.01,now+0.1);oscillator.start(now);oscillator.stop(now+0.1);}catch(e){console.error('Error playing shoot sound:',e);}}function playExplosionSound(){try{if(!audioContext)return;const now=audioContext.currentTime;const oscillator1=audioContext.createOscillator();const oscillator2=audioContext.createOscillator();const gainNode=audioContext.createGain();oscillator1.connect(gainNode);oscillator2.connect(gainNode);gainNode.connect(audioContext.destination);oscillator1.type='sawtooth';oscillator2.type='square';oscillator1.frequency.setValueAtTime(120,now);oscillator1.frequency.exponentialRampToValueAtTime(40,now+0.3);oscillator2.frequency.setValueAtTime(80,now);oscillator2.frequency.exponentialRampToValueAtTime(30,now+0.3);gainNode.gain.setValueAtTime(0.4,now);gainNode.gain.exponentialRampToValueAtTime(0.01,now+0.3);oscillator1.start(now);oscillator1.stop(now+0.3);oscillator2.start(now);oscillator2.stop(now+0.3);}catch(e){console.error('Error playing explosion sound:',e);}}function playEnemyHitSound(){try{if(!audioContext)return;const now=audioContext.currentTime;const oscillator=audioContext.createOscillator();const gainNode=audioContext.createGain();oscillator.connect(gainNode);gainNode.connect(audioContext.destination);oscillator.type='triangle';oscillator.frequency.setValueAtTime(1200", "chunk-1": ",now);oscillator.frequency.exponentialRampToValueAtTime(800,now+0.05);gainNode.gain.setValueAtTime(0.3,now);gainNode.gain.exponentialRampToValueAtTime(0.01,now+0.05);oscillator.start(now);oscillator.stop(now+0.05);}catch(e){console.error('Error playing enemy hit sound:',e);}}function playTriumphSound(){try{if(!audioContext)return;const now=audioContext.currentTime;const oscillator=audioContext.createOscillator();const gainNode=audioContext.createGain();oscillator.connect(gainNode);gainNode.connect(audioContext.destination);oscillator.type='square';oscillator.frequency.setValueAtTime(262,now);oscillator.frequency.setValueAtTime(330,now+0.1);oscillator.frequency.setValueAtTime(392,now+0.2);gainNode.gain.setValueAtTime(0.3,now);gainNode.gain.setValueAtTime(0.3,now+0.2);gainNode.gain.exponentialRampToValueAtTime(0.01,now+0.4);oscillator.start(now);oscillator.stop(now+0.4);}catch(e){console.error('Error playing triumph sound:',e);}}function playPlayerDeathSound(){try{if(!audioContext)return;const now=audioContext.currentTime;const oscillator1=audioContext.createOscillator();const oscillator2=audioContext.createOscillator();const oscillator3=audioContext.createOscillator();const gainNode=audioContext.createGain();oscillator1.connect(gainNode);oscillator2.connect(gainNode);oscillator3.connect(gainNode);gainNode.connect(audioContext.destination);oscillator1.type='sawtooth';oscillator2.type='square';oscillator3.type='triangle';oscillator1.frequency.setValueAtTime(300,now);oscillator1.frequency.exponentialRampToValueAtTime(50,now+0.5);oscillator2.frequency.setValueAtTime(180,now);oscillator2.frequency.exponentialRampToValueAtTime(30,now+0.5);oscillator3.frequency.setValueAtTime(450,now);oscillator3.frequency.exponentialRampToValueAtTime(60,now+0.5);gainNode.gain.setValueAtTime(0.5,now);gainNode.gain.exponentialRampToValueAtTime(0.01,now+0.5);oscillator1.start(now);oscillator1.stop(now+0.5);oscillator2.start(now);oscillator2.stop(now+0.5);oscillator3.start(now);oscillator3.stop(now+0.5);}catch(e){console.error('Error playing player death sound:',e);}}function playEnemyDeathSound(){try{if(!audioContext)return;const now=audioContext.currentTime;const oscillator1=audioContext.createOscillator();const oscillator2=audioContext.createOscillator();const gainNode=audioContext.createGain();oscillator1.connect(gainNode);oscillator2.connect(gainNode);gainNode.connect(audioContext.destination);oscillator1.type='square';oscillator2.type='sawtooth';oscillator1.frequency.setValueAtTime(600,now);oscillator1.frequency.exponentialRampToValueAtTime(100,now+0.4);oscillator2.frequency.setValueAtTime(350,now);oscillator2.frequency.exponentialRampToValueAtTime(60,now+0.4);gainNode.gain.setValueAtTime(0.45,now);gainNode.gain.exponentialRampToValueAtTime(0.01,now+0.4);oscillator1.start(now);oscillator1.stop(now+0.4);oscillator2.start(now);oscillator2.stop(now+0.4);}catch(e){console.error('Error playing enemy death sound:',e);}}function playExtraLifeSound(){try{if(!audioContext)return;const now=audioContext.currentTime;const oscillator=audioContext.createOscillator();const gainNode=audioContext.createGain();oscillator.connect(gainNode);gainNode.connect(audioContext.destination);oscillator.type='square';oscillator.frequency.setValueAtTime(440,now);oscillator.frequency.setValueAtTime(554,now+0.08);oscillator.frequency.setValueAtTime(659,now+0.16);gainNode.gain.setValueAtTime(0.25,now);gainNode.gain.setValueAtTime(0.25,now+0.16);gainNode.gain.exponentialRampToValueAtTime(0.01,now+0.3);oscillator.start(now);oscillator.stop(now+0.3);}catch(e){console.error('Error playing extra life sound:',e);}}function playMEVShootSound(){try{if(!audioContext)return;const now=audioContext.currentTime;const oscillator=audioContext.createOscillator();const gainNode=audioContext.createGain();oscillator.connect(gainNode);gainNode.connect(audioContext.destination);oscillator.type='sine';oscillator.frequency.setValueAtTime(500,now);oscillator.frequency.exponentialRampToValueAtTime(150,now+0.15);gainNode.gain.setValueAtTime(0.25,now);gainNode.gain.exponentialRampToValueAtTime(0.01,now+0.15);oscillator.start(now);oscillator.stop(now+0.15);}catch(e){console.error('Error playing MEV shoot sound:',e);}}const scale={x:(relativeValue)=>canvas.width*relativeValue,y:(relativeValue)=>canvas.height*relativeValue,size:(relativeValue)=>Math.min(canvas.width,canvas.height)*relativeValue};const player={x:0.5,y:0.5,velocityX:0,velocityY:0,rotation:0,size:0.03,color:'#ffffff',acceleration:0.0002,rotationSpeed:0.08,friction:0.98,brakeForce:0.95};const keys={ArrowUp:false,ArrowDown:false,ArrowLeft:false,ArrowRight:false,'w':false,'s':false,'a':false,'d':false,' ':false};let gamepadThrusting=false;const touchControls={joystick:{active:false,startX:0,startY:0,currentX:0,currentY:0,identifier:null},shootButton:{active:false,identifier:null}};const joystickRadius=60;const joystickHandleRadius=25;const shootButtonRadius=40;let gamepadIndex=null;let lastShootButtonState=false;let lastGamepadBut", "chunk-2": "tonStates=[];window.addEventListener('gamepadconnected',(e)=>{console.log('Gamepad connected:',e.gamepad.id);gamepadIndex=e.gamepad.index;});window.addEventListener('gamepaddisconnected',(e)=>{console.log('Gamepad disconnected');if(gamepadIndex===e.gamepad.index){gamepadIndex=null;}});function getGamepadInput(){if(gamepadIndex===null)return null;const gamepads=navigator.getGamepads();return gamepads[gamepadIndex];}const projectiles=[];let canShoot=true;const asteroids=[{x:0.3,y:0.2,velocityX:0.0005,velocityY:0.0003,size:0.06,color:'#e5e7e7',rotation:0,rotationSpeed:0.02},{x:0.7,y:0.6,velocityX:-0.0003,velocityY:0.0004,size:0.06,color:'#e5e7e7',rotation:0,rotationSpeed:-0.015}];let gameStarted=false;let gameOver=false;let gameOverTime=null;let paused=false;let lives=3;let score=0;let level=1;let nextLifeAt=5000;let invincible=false;let invincibleTimer=0;let levelTransition=false;let lastTime=performance.now();const targetFPS=60;const targetFrameTime=1000/targetFPS;let mevEnemies=[];let mevDefeats=0;let mevIdCounter=0;const mevProjectiles=[];let mevShootCooldown=0;function resizeCanvas(){const aspectRatio=4/3;const maxWidth=window.innerWidth-20;const maxHeight=window.innerHeight-20;let width=maxWidth;let height=width/aspectRatio;if(height>maxHeight){height=maxHeight;width=height*aspectRatio;}canvas.width=width;canvas.height=height;draw();}function drawStartScreen(){ctx.clearRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#ffffff';ctx.font=scale.size(0.12)+'px \"Courier New\",monospace';ctx.textAlign='center';ctx.fillText('VERUS BLASTER',canvas.width/2,canvas.height*0.2);const shipSize=scale.size(0.15);ctx.save();ctx.translate(canvas.width/2,canvas.height/2-40);ctx.rotate(-3*Math.PI/4);ctx.fillStyle='#ffffff';ctx.beginPath();ctx.moveTo(shipSize,0);ctx.lineTo(-shipSize,shipSize);ctx.lineTo(-shipSize,shipSize*0.5);ctx.lineTo(-shipSize*0.7,0);ctx.lineTo(-shipSize,-shipSize*0.5);ctx.lineTo(-shipSize,-shipSize);ctx.closePath();ctx.fill();ctx.restore();ctx.font=scale.size(0.025)+'px \"Courier New\",monospace';ctx.fillText('An Onchain HTML5 Arcade Game Running on the Verus Protocol',canvas.width/2,canvas.height*0.75);ctx.fillText('by Tommy Faldt Pedersen',canvas.width/2,canvas.height*0.8);ctx.font=scale.size(0.04)+'px \"Courier New\",monospace';const isTouchDevice='ontouchstart' in window||navigator.maxTouchPoints>0;const startText=isTouchDevice?'Tap to Start':'Press Any Key or Press Controller Button to Start';ctx.fillText(startText,canvas.width/2,canvas.height*0.9);}function draw(){if(!gameStarted){drawStartScreen();return;}ctx.clearRect(0,0,canvas.width,canvas.height);drawAsteroids();if(mevEnemies.length>0){drawMEV();drawMEVProjectiles();}drawProjectiles();drawLives();drawLevel();drawScore();if(!gameOver){drawSpaceship();}else{drawExplosion();}if(paused&&!gameOver){drawPauseScreen();}}function drawSpaceship(){if(invincible&&Math.floor(Date.now()/100)%2===0){return;}const x=scale.x(player.x);const y=scale.y(player.y);const size=scale.size(player.size);ctx.save();ctx.translate(x,y);ctx.rotate(player.rotation);ctx.fillStyle=player.color;ctx.beginPath();ctx.moveTo(size,0);ctx.lineTo(-size,size);ctx.lineTo(-size,size*0.5);ctx.lineTo(-size*0.7,0);ctx.lineTo(-size,-size*0.5);ctx.lineTo(-size,-size);ctx.closePath();ctx.fill();if(keys.ArrowUp||keys['w']||gamepadThrusting){ctx.fillStyle='#ffffff';ctx.beginPath();ctx.moveTo(-size*0.7,size*0.5);ctx.lineTo(-size*1.5,0);ctx.lineTo(-size*0.7,-size*0.5);ctx.lineTo(-size*0.7,0);ctx.closePath();ctx.fill();}ctx.restore();}function drawLives(){const startX=scale.size(0.03);const startY=scale.size(0.03);const shipSize=scale.size(0.015);const spacing=scale.size(0.04);const livesToDisplay=Math.min(lives,10);for(let i=0;i<livesToDisplay;i++){const x=startX+(i*spacing);const y=startY;ctx.save();ctx.translate(x,y);ctx.rotate(-Math.PI/2);ctx.fillStyle='#ffffff';ctx.beginPath();ctx.moveTo(shipSize,0);ctx.lineTo(-shipSize,shipSize);ctx.lineTo(-shipSize,shipSize*0.5);ctx.lineTo(-shipSize*0.7,0);ctx.lineTo(-shipSize,-shipSize*0.5);ctx.lineTo(-shipSize,-shipSize);ctx.closePath();ctx.fill();ctx.restore();}if(lives>10){const plusX=startX+(10*spacing)-14;const plusY=startY+scale.size(0.006)+5;ctx.fillStyle='#ffffff';ctx.font=scale.size(0.04)+'px \"Courier New\",monospace';ctx.textAlign='left';ctx.fillText('+',plusX,plusY);}}function drawScore(){ctx.fillStyle='#ffffff';ctx.font=scale.size(0.04)+'px \"Courier New\",monospace';ctx.textAlign='right';ctx.fillText(score.toString().padStart(8,'0'),canvas.width-scale.size(0.03),scale.size(0.04));}function drawLevel(){ctx.fillStyle='#ffffff';ctx.font=scale.size(0.05)+'px \"Courier New\",monospace';ctx.textAlign='center';ctx.fillText(level.toString().padStart(3,'0'),canvas.width/2,scale.size(0.05));}function drawAsteroids(){asteroids.forEach(asteroid=>{const size=scale.size(asteroid.size);const x=scale.x(asteroid.x);const y=scale.y(asteroid.y);const width=size*2;const height=size*2;const radius=size*0.2;ctx.save();ctx.translate(x,y);ctx.rotate(asteroid.rotation);ctx", "chunk-3": ".fillStyle=asteroid.color;ctx.beginPath();ctx.roundRect(-size,-size,width,height,radius);ctx.fill();ctx.strokeStyle=asteroid.color;ctx.lineWidth=2;ctx.stroke();ctx.restore();});}function drawMEV(){mevEnemies.forEach(mev=>{const x=scale.x(mev.x);const y=scale.y(mev.y);const size=scale.size(mev.size);ctx.save();ctx.translate(x,y);ctx.rotate(mev.rotation);ctx.fillStyle='#e5e7e7';ctx.beginPath();const radius=size;const cornerRadius=size*0.15;for(let i=0;i<6;i++){const angle=(Math.PI/3)*i-Math.PI/2;const nextAngle=(Math.PI/3)*(i+1)-Math.PI/2;const x1=Math.cos(angle)*radius;const y1=Math.sin(angle)*radius;const x2=Math.cos(nextAngle)*radius;const y2=Math.sin(nextAngle)*radius;if(i===0){ctx.moveTo(x1,y1);}ctx.arcTo(x2,y2,Math.cos(nextAngle+Math.PI/3)*radius,Math.sin(nextAngle+Math.PI/3)*radius,cornerRadius);}ctx.closePath();ctx.fill();ctx.save();ctx.rotate(-Math.PI/2);ctx.fillStyle='#000000';ctx.font=scale.size(0.0225)+'px \"Courier New\",monospace';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText('MEV',0,0);ctx.restore();ctx.restore();});}function drawMEVProjectiles(){ctx.fillStyle='#ff6666';mevProjectiles.forEach(proj=>{ctx.beginPath();ctx.arc(scale.x(proj.x),scale.y(proj.y),scale.size(0.006),0,Math.PI*2);ctx.fill();});}function drawExplosion(){const x=scale.x(player.x);const y=scale.y(player.y);const size=scale.size(player.size);const elapsedTime=performance.now()-gameOverTime;const fadeDuration=1000;const opacity=Math.max(0,1-(elapsedTime/fadeDuration));ctx.fillStyle=`rgba(255,255,255,${opacity})`;for(let i=0;i<8;i++){const angle=(Math.PI*2/8)*i;ctx.beginPath();ctx.arc(x+Math.cos(angle)*size*2,y+Math.sin(angle)*size*2,size*0.5,0,Math.PI*2);ctx.fill();}ctx.fillStyle='#ffffff';ctx.font=scale.size(0.06)+'px \"Courier New\",monospace';ctx.textAlign='center';ctx.fillText('GAME OVER',canvas.width/2,canvas.height/2);ctx.font=scale.size(0.03)+'px \"Courier New\",monospace';const isTouchDevice='ontouchstart' in window||navigator.maxTouchPoints>0;const restartText=isTouchDevice?'Tap to Restart':'Press R to Restart';ctx.fillText(restartText,canvas.width/2,canvas.height/2+scale.size(0.08));}function drawPauseScreen(){ctx.fillStyle='rgba(0,0,0,0.5)';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#ffffff';ctx.font=scale.size(0.06)+'px \"Courier New\",monospace';ctx.textAlign='center';ctx.fillText('PAUSED',canvas.width/2,canvas.height/2);ctx.font=scale.size(0.03)+'px \"Courier New\",monospace';const resumeText=gamepadIndex!==null?'Press Select to Resume':'Press ESC or P to Resume';ctx.fillText(resumeText,canvas.width/2,canvas.height/2+scale.size(0.08));}function drawProjectiles(){ctx.fillStyle='#ffffff';projectiles.forEach(proj=>{ctx.beginPath();ctx.arc(scale.x(proj.x),scale.y(proj.y),scale.size(0.005),0,Math.PI*2);ctx.fill();});}function shoot(){if(!canShoot)return;const projectileSpeed=0.008;const frontX=player.x+Math.cos(player.rotation)*player.size;const frontY=player.y+Math.sin(player.rotation)*player.size;projectiles.push({x:frontX,y:frontY,velocityX:player.velocityX+Math.cos(player.rotation)*projectileSpeed,velocityY:player.velocityY+Math.sin(player.rotation)*projectileSpeed,lifetime:0});playShootSound();canShoot=false;setTimeout(()=>canShoot=true,200);}function updateProjectiles(timeScale=1){for(let i=projectiles.length-1;i>=0;i--){const proj=projectiles[i];proj.x+=proj.velocityX*timeScale;proj.y+=proj.velocityY*timeScale;proj.lifetime+=timeScale;let hitAsteroid=false;for(let j=asteroids.length-1;j>=0;j--){const asteroid=asteroids[j];const dx=proj.x-asteroid.x;const dy=proj.y-asteroid.y;const distance=Math.sqrt(dx*dx+dy*dy);if(distance<asteroid.size){hitAsteroid=true;if(asteroid.size>=0.06){score+=100;}else if(asteroid.size>=0.03){score+=50;}else{score+=25;}window.parent.postMessage({type:'GAME_UPDATE',game:'Verus Blaster',lives:lives,level:level,score:score},'*');if(score>=nextLifeAt){lives++;nextLifeAt+=5000;playExtraLifeSound();window.parent.postMessage({type:'GAME_UPDATE',game:'Verus Blaster',lives:lives,level:level,score:score},'*');}if(asteroid.size>0.02){const newSize=asteroid.size/2;const speed=Math.sqrt(asteroid.velocityX**2+asteroid.velocityY**2)*2;for(let k=0;k<2;k++){const angle=Math.random()*Math.PI*2;asteroids.push({x:asteroid.x,y:asteroid.y,velocityX:Math.cos(angle)*speed,velocityY:Math.sin(angle)*speed,size:newSize,color:asteroid.color,rotation:Math.random()*Math.PI*2,rotationSpeed:(Math.random()-0.5)*0.04});}}asteroids.splice(j,1);playExplosionSound();break;}}if(hitAsteroid||proj.x<0||proj.x>1||proj.y<0||proj.y>1||proj.lifetime>62){projectiles.splice(i,1);}}}function updateAsteroids(timeScale=1){asteroids.forEach(asteroid=>{asteroid.x+=asteroid.velocityX*timeScale;asteroid.y+=asteroid.velocityY*timeScale;asteroid.rotation+=asteroid.rotationSpeed*timeScale;if(asteroid.x<-asteroid.size)asteroid.x=1+asteroid.size;if(asteroid.x>1+asteroid.size)asteroid.x=-asteroid.size;if(asteroid.y<-asteroid.size)asteroid.y=1+asteroid.size;if(asteroid.y>1+asteroid.size)asteroid.y=-asteroid.size;});}funct", "chunk-4": "ion updateMEV(timeScale=1){mevEnemies.forEach(mev=>{mev.x+=mev.velocityX*timeScale;mev.y+=mev.velocityY*timeScale;if(mev.x<-mev.size)mev.x=1+mev.size;if(mev.x>1+mev.size)mev.x=-mev.size;if(mev.y<-mev.size)mev.y=1+mev.size;if(mev.y>1+mev.size)mev.y=-mev.size;const angleToPlayer=Math.atan2(player.y-mev.y,player.x-mev.x);mev.rotation=angleToPlayer;mev.shootCooldown-=timeScale;if(mev.shootCooldown<=0&&Math.random()<0.01){const projectileSpeed=0.006;mevProjectiles.push({x:mev.x,y:mev.y,velocityX:Math.cos(mev.rotation)*projectileSpeed,velocityY:Math.sin(mev.rotation)*projectileSpeed,lifetime:0,mevId:mev.id});playMEVShootSound();mev.shootCooldown=120+Math.random()*240;}});}function mevShoot(){const mev=mevEnemies[Math.floor(Math.random()*mevEnemies.length)];if(!mev)return;const projectileSpeed=0.006;mevProjectiles.push({x:mev.x,y:mev.y,velocityX:Math.cos(mev.rotation)*projectileSpeed,velocityY:Math.sin(mev.rotation)*projectileSpeed,lifetime:0,mevId:mev.id});}function updateMEVProjectiles(timeScale=1){for(let i=mevProjectiles.length-1;i>=0;i--){const proj=mevProjectiles[i];proj.x+=proj.velocityX*timeScale;proj.y+=proj.velocityY*timeScale;proj.lifetime+=timeScale;if(!invincible&&!gameOver){const dx=proj.x-player.x;const dy=proj.y-player.y;const distance=Math.sqrt(dx*dx+dy*dy);if(distance<player.size+0.006){lives--;mevProjectiles.splice(i,1);playPlayerDeathSound();window.parent.postMessage({type:'GAME_UPDATE',game:'Verus Blaster',lives:lives,level:level,score:score},'*');if(lives<=0){gameOver=true;gameOverTime=performance.now();}else{respawnPlayer();}continue;}}if(proj.x<0||proj.x>1||proj.y<0||proj.y>1||proj.lifetime>180){mevProjectiles.splice(i,1);}}}function checkMEVCollision(){if(mevEnemies.length===0)return;for(let i=projectiles.length-1;i>=0;i--){const proj=projectiles[i];let hitMEV=false;for(let m=mevEnemies.length-1;m>=0;m--){const mev=mevEnemies[m];const dx=proj.x-mev.x;const dy=proj.y-mev.y;const distance=Math.sqrt(dx*dx+dy*dy);if(distance<mev.size){mev.health--;projectiles.splice(i,1);hitMEV=true;if(mev.health<=0){const destroyedMevId=mev.id;mevEnemies.splice(m,1);mevDefeats++;for(let p=mevProjectiles.length-1;p>=0;p--){if(mevProjectiles[p].mevId===destroyedMevId){mevProjectiles.splice(p,1);}}score+=500;if(score>=nextLifeAt){lives++;nextLifeAt+=5000;playExtraLifeSound();window.parent.postMessage({type:'GAME_UPDATE',game:'Verus Blaster',lives:lives,level:level,score:score},'*');}playEnemyDeathSound();if(asteroids.length===0&&mevEnemies.length===0){levelTransition=true;setTimeout(()=>{if(!gameOver){nextLevel();}},100);}}else{playEnemyHitSound();}break;}}if(hitMEV)break;}}function nextLevel(){if(level>=999)return;levelTransition=true;level++;playTriumphSound();window.parent.postMessage({type:'GAME_UPDATE',game:'Verus Blaster',lives:lives,level:level,score:score},'*');if(level%3===0){const mevsToSpawn=1+Math.floor(mevDefeats/3);for(let m=0;m<mevsToSpawn;m++){const angle=Math.random()*Math.PI*2;const speed=0.0004+(Math.random()*0.0002);mevEnemies.push({id:mevIdCounter++,x:Math.random(),y:Math.random(),velocityX:Math.cos(angle)*speed,velocityY:Math.sin(angle)*speed,rotation:0,size:0.04,health:5,shootCooldown:60+Math.random()*120});}}const asteroidsToSpawn=2+Math.floor((level-1)/3);for(let i=0;i<asteroidsToSpawn;i++){let x,y,distance;const minDistance=0.3;let attempts=0;const maxAttempts=100;do{x=Math.random();y=Math.random();const dx=x-player.x;const dy=y-player.y;distance=Math.sqrt(dx*dx+dy*dy);attempts++;if(attempts>=maxAttempts){break;}}while(distance<minDistance);const angle=Math.random()*Math.PI*2;const speed=0.0004+(Math.random()*0.0002);asteroids.push({x:x,y:y,velocityX:Math.cos(angle)*speed,velocityY:Math.sin(angle)*speed,size:0.06,color:'#e5e7e7',rotation:Math.random()*Math.PI*2,rotationSpeed:(Math.random()-0.5)*0.04});}invincible=true;invincibleTimer=120;levelTransition=false;}function checkCollisions(){if(gameOver||invincible)return;asteroids.forEach(asteroid=>{const dx=player.x-asteroid.x;const dy=player.y-asteroid.y;const distance=Math.sqrt(dx*dx+dy*dy);if(distance<player.size+asteroid.size){lives--;playPlayerDeathSound();window.parent.postMessage({type:'GAME_UPDATE',game:'Verus Blaster',lives:lives,level:level,score:score},'*');if(lives<=0){gameOver=true;gameOverTime=performance.now();}else{respawnPlayer();}}});}function respawnPlayer(){player.velocityX=0;player.velocityY=0;invincible=true;invincibleTimer=180;}function resetGame(){player.x=0.5;player.y=0.5;player.velocityX=0;player.velocityY=0;player.rotation=0;gameOver=false;lives=3;score=0;level=1;nextLifeAt=5000;invincible=false;invincibleTimer=0;window.parent.postMessage({type:'GAME_UPDATE',game:'Verus Blaster',lives:lives,level:level,score:score},'*');levelTransition=false;projectiles.length=0;mevEnemies.length=0;mevDefeats=0;mevProjectiles.length=0;mevShootCooldown=0;asteroids.length=0;asteroids.push({x:0.3,y:0.2,velocityX:0.0005,velocityY:0.0003,size:0.06,color:'#e5e7e7',rotation:0,rotationSpeed:0.02},{x:0.7,y:0.6,velocityX:-0.0003,velocityY:", "chunk-5": "0.0004,size:0.06,color:'#e5e7e7',rotation:0,rotationSpeed:-0.015});}function updatePlayer(timeScale=1){if(gameOver)return;gamepadThrusting=false;if(invincible){invincibleTimer-=timeScale;if(invincibleTimer<=0){invincible=false;}}const gamepad=getGamepadInput();const gamepadShoot=gamepad&&(gamepad.buttons[0].pressed||gamepad.buttons[2].pressed||gamepad.buttons[7].pressed);if(keys[' ']||touchControls.shootButton.active||gamepadShoot){shoot();}let usingGamepad=false;if(gamepad){const leftStickX=gamepad.axes[0];const leftStickY=gamepad.axes[1];const deadzone=0.15;if(Math.abs(leftStickX)>deadzone||Math.abs(leftStickY)>deadzone){usingGamepad=true;const angle=Math.atan2(leftStickY,leftStickX);const magnitude=Math.sqrt(leftStickX*leftStickX+leftStickY*leftStickY);const targetRotation=angle;let rotationDiff=targetRotation-player.rotation;while(rotationDiff>Math.PI)rotationDiff-=Math.PI*2;while(rotationDiff<-Math.PI)rotationDiff+=Math.PI*2;if(Math.abs(rotationDiff)>0.05){player.rotation+=Math.sign(rotationDiff)*player.rotationSpeed*timeScale;}const thrustAmount=Math.min(magnitude,1);player.velocityX+=Math.cos(player.rotation)*player.acceleration*timeScale*thrustAmount;player.velocityY+=Math.sin(player.rotation)*player.acceleration*timeScale*thrustAmount;gamepadThrusting=thrustAmount>0.3;}if(gamepad.buttons[6].pressed||gamepad.buttons[1].pressed){player.velocityX*=Math.pow(player.brakeForce,timeScale);player.velocityY*=Math.pow(player.brakeForce,timeScale);}}if(!usingGamepad&&touchControls.joystick.active){const dx=touchControls.joystick.currentX-touchControls.joystick.startX;const dy=touchControls.joystick.currentY-touchControls.joystick.startY;const distance=Math.sqrt(dx*dx+dy*dy);if(distance>10){const angle=Math.atan2(dy,dx);const targetRotation=angle;let rotationDiff=targetRotation-player.rotation;while(rotationDiff>Math.PI)rotationDiff-=Math.PI*2;while(rotationDiff<-Math.PI)rotationDiff+=Math.PI*2;if(Math.abs(rotationDiff)>0.05){player.rotation+=Math.sign(rotationDiff)*player.rotationSpeed*timeScale;}const thrustAmount=Math.min(distance/joystickRadius,1);player.velocityX+=Math.cos(player.rotation)*player.acceleration*timeScale*thrustAmount*1;player.velocityY+=Math.sin(player.rotation)*player.acceleration*timeScale*thrustAmount*1;gamepadThrusting=thrustAmount>0.3;}}else if(!usingGamepad){if(keys.ArrowLeft||keys['a']){player.rotation-=player.rotationSpeed*timeScale;}if(keys.ArrowRight||keys['d']){player.rotation+=player.rotationSpeed*timeScale;}if(keys.ArrowUp||keys['w']){player.velocityX+=Math.cos(player.rotation)*player.acceleration*timeScale;player.velocityY+=Math.sin(player.rotation)*player.acceleration*timeScale;}if(keys.ArrowDown||keys['s']){player.velocityX*=Math.pow(player.brakeForce,timeScale);player.velocityY*=Math.pow(player.brakeForce,timeScale);}}player.velocityX*=Math.pow(player.friction,timeScale);player.velocityY*=Math.pow(player.friction,timeScale);player.x+=player.velocityX*timeScale;player.y+=player.velocityY*timeScale;if(player.x<0)player.x=1;if(player.x>1)player.x=0;if(player.y<0)player.y=1;if(player.y>1)player.y=0;}window.addEventListener('keydown',(e)=>{initAudio();if(!gameStarted){gameStarted=true;e.preventDefault();return;}if(e.key in keys){keys[e.key]=true;e.preventDefault();}if(e.key===' '||e.code==='Space'){keys[' ']=true;e.preventDefault();}if((e.key==='Escape'||e.key==='p'||e.key==='P')&&!gameOver){paused=!paused;e.preventDefault();}if(e.key==='r'||e.key==='R'){if(gameOver)resetGame();}});window.addEventListener('keyup',(e)=>{if(e.key in keys){keys[e.key]=false;e.preventDefault();}if(e.key===' '||e.code==='Space'){keys[' ']=false;e.preventDefault();}});resizeCanvas();window.addEventListener('resize',resizeCanvas);const joystickElement=document.getElementById('joystick');const joystickHandle=document.getElementById('joystick-handle');const shootButtonElement=document.getElementById('shoot-button');const pauseButtonElement=document.getElementById('pause-button');joystickElement.addEventListener('touchstart',(e)=>{e.preventDefault();const touch=e.changedTouches[0];const rect=joystickElement.getBoundingClientRect();touchControls.joystick.active=true;touchControls.joystick.identifier=touch.identifier;touchControls.joystick.startX=rect.left+rect.width/2;touchControls.joystick.startY=rect.top+rect.height/2;touchControls.joystick.currentX=touch.clientX;touchControls.joystick.currentY=touch.clientY;updateJoystickVisual();});joystickElement.addEventListener('touchmove',(e)=>{e.preventDefault();if(!touchControls.joystick.active)return;for(let touch of e.touches){if(touch.identifier===touchControls.joystick.identifier){touchControls.joystick.currentX=touch.clientX;touchControls.joystick.currentY=touch.clientY;updateJoystickVisual();break;}}});joystickElement.addEventListener('touchend',(e)=>{e.preventDefault();touchControls.joystick.active=false;touchControls.joystick.identifier=null;joystickHandle.style.left='50%';joystickHandle.style.top='50%';});shootButtonElement.addEventListener('touchstart',", "chunk-6": "(e)=>{e.preventDefault();initAudio();touchControls.shootButton.active=true;touchControls.shootButton.identifier=e.changedTouches[0].identifier;shootButtonElement.classList.add('active');});shootButtonElement.addEventListener('touchend',(e)=>{e.preventDefault();touchControls.shootButton.active=false;touchControls.shootButton.identifier=null;shootButtonElement.classList.remove('active');});shootButtonElement.addEventListener('touchcancel',(e)=>{e.preventDefault();touchControls.shootButton.active=false;touchControls.shootButton.identifier=null;shootButtonElement.classList.remove('active');});pauseButtonElement.addEventListener('touchstart',(e)=>{e.preventDefault();if(!gameOver){paused=!paused;pauseButtonElement.classList.toggle('active',paused);}});function updateJoystickVisual(){const dx=touchControls.joystick.currentX-touchControls.joystick.startX;const dy=touchControls.joystick.currentY-touchControls.joystick.startY;const distance=Math.sqrt(dx*dx+dy*dy);const maxDistance=35;if(distance>maxDistance){const angle=Math.atan2(dy,dx);joystickHandle.style.left=(50+(Math.cos(angle)*maxDistance*100/60))+'%';joystickHandle.style.top=(50+(Math.sin(angle)*maxDistance*100/60))+'%';}else{joystickHandle.style.left=(50+(dx*100/60))+'%';joystickHandle.style.top=(50+(dy*100/60))+'%';}}canvas.addEventListener('touchstart',(e)=>{if(!gameStarted){e.preventDefault();initAudio();gameStarted=true;return;}if(gameOver){e.preventDefault();resetGame();}});function gameLoop(currentTime){const deltaTime=currentTime-lastTime;lastTime=currentTime;const timeScale=deltaTime/targetFrameTime;const gamepad=getGamepadInput();if(gamepad){if(!gameStarted){for(let i=0;i<gamepad.buttons.length;i++){const currentButtonState=gamepad.buttons[i].pressed;const previousButtonState=lastGamepadButtonStates[i]||false;if(currentButtonState&&!previousButtonState){console.log('Game started via gamepad button:',i);initAudio();gameStarted=true;break;}}}if(gameOver){for(let i=0;i<gamepad.buttons.length;i++){const currentButtonState=gamepad.buttons[i].pressed;const previousButtonState=lastGamepadButtonStates[i]||false;if(currentButtonState&&!previousButtonState){console.log('Game restarted via gamepad button:',i);resetGame();break;}}}if(gameStarted&&!gameOver){const selectButtonState=gamepad.buttons[8]?gamepad.buttons[8].pressed:false;const selectButtonPreviousState=lastGamepadButtonStates[8]||false;if(selectButtonState&&!selectButtonPreviousState){paused=!paused;console.log('Pause toggled via Select button:',paused);}}lastGamepadButtonStates=gamepad.buttons.map(button=>button.pressed);}if(gameStarted&&!paused&&timeScale>0&&timeScale<3){updatePlayer(timeScale);updateProjectiles(timeScale);updateAsteroids(timeScale);updateMEV(timeScale);updateMEVProjectiles(timeScale);checkCollisions();checkMEVCollision();if(!gameOver&&!levelTransition&&asteroids.length===0&&mevEnemies.length===0){nextLevel();}}draw();requestAnimationFrame(gameLoop);}lastTime=performance.now();gameLoop(lastTime);</script></body></html>"}